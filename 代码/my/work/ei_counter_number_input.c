#include <stdio.h>
int main()
{
    int i = 0;
    int counter = 0;
    char ch;
    char cha;
   
    while((scanf("%c",&ch) != '\n') && (ch != '#'))
    {
        if(ch == 'i')
        {

        if(cha == 'e')
        {
            counter ++;
            
        }
        
        }
        cha = ch;
    }
    printf("%d",counter);
    return 0;
   
}
